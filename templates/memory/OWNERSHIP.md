# Ownership map (starter)

Copy into the product project as `memory/OWNERSHIP.md` and replace with real owners.

| Capability | Owner |
| --- | --- |
| Authentication | `shared/auth` |
| Example feature | `features/<name>` |
| Example page | `pages/<name>` |

Agents must read this before editing and keep it current after verified ownership changes.
