name: database
purpose: optional schema and migration-focused work
trigger:
  - database
  - migration
  - schema
requires:
  - backend-need
outputs:
  - migrations
  - verification-report
optional: true

inputs:
  - agreed task scope and existing project context
dependencies: []
verification:
  - evidence for declared outputs and relevant project checks
