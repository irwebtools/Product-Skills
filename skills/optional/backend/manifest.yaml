name: backend
purpose: optional non-Supabase API or server work
trigger:
  - backend
  - API
  - server
requires:
  - backend-need
outputs:
  - API contracts
  - verification-report
optional: true

inputs:
  - agreed task scope and existing project context
dependencies: []
verification:
  - evidence for declared outputs and relevant project checks
