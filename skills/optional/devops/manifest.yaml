name: devops
purpose: optional authorized environment or deployment work
trigger:
  - devops
  - deploy
  - preview environment
requires:
  - explicit-authorization
outputs:
  - deployment evidence
  - health verification
optional: true

inputs:
  - agreed task scope and existing project context
dependencies: []
verification:
  - evidence for declared outputs and relevant project checks
