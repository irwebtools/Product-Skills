name: react
purpose: frontend implementation with TypeScript and Feature-Sliced Design
trigger:
  - React
  - frontend
  - TypeScript
  - FSD
requires:
  - architecture-check
outputs:
  - implementation
  - verification-report

inputs:
  - agreed task scope and existing project context
dependencies: []
verification:
  - evidence for declared outputs and relevant project checks
