# Runtime folder: `.agents`

Optional integration directory for agent runtimes that look for `.agents/` configuration.

Canonical Product-Skills behavior lives in:

- `AGENTS.md`
- `skills/`
- `rules/`
- `workflows/`

Do not duplicate skill instructions here. Keep this folder for runtime wiring only.
